Illustration layout for Castle Ledger:

- `base/` contains the base game room illustrations, named by room id.
- `dlc/battle-of-mastings/` is reserved for battle DLC room illustrations.
- `dlc/harbor-of-delays/` is reserved for harbor DLC room illustrations.

Keep image filenames aligned to location ids so `game.js` can reference them predictably.
